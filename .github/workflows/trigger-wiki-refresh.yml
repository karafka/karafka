name: Trigger Wiki Refresh

on:
  release:
    types: [published]
  push:
    branches: [master]

jobs:
  trigger-wiki-refresh:
    runs-on: ubuntu-latest
    environment: wiki-trigger
    steps:
      - name: Trigger wiki refresh
        uses: peter-evans/repository-dispatch@5a3edc67490343006aa8cc8e4a7c69ef6094d733
        with:
          token: ${{ secrets.WIKI_REPO_TOKEN }}
          repository: karafka/wiki
          event-type: sync-trigger
          client-payload: |
            {
              "repository": "${{ github.repository }}",
              "event_name": "${{ github.event_name }}",
              "release_tag": "${{ github.event.release.tag_name || '' }}",
              "release_name": "${{ github.event.release.name || '' }}",
              "commit_sha": "${{ github.sha }}",
              "commit_message": "${{ github.event.head_commit.message || '' }}",
              "triggered_by": "${{ github.actor }}"
            }
