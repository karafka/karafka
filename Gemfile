source 'https://rubygems.org'

gemspec

# TODO: move back to *.gemspec after merge
gem 'waterdrop', git: 'https://github.com/mach-kernel/waterdrop.git', branch: 'add-topic-prefix'
  
group :development, :test do
  gem 'polishgeeks-dev-tools'
  gem 'timecop'
end
